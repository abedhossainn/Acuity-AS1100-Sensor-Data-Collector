FROM node:20-alpine

WORKDIR /app

# Install dependencies first (cached layer)
COPY package.json package-lock.json* ./
RUN npm ci || npm install

# Source is mounted at runtime — no COPY needed here
EXPOSE 3000

CMD ["npm", "run", "dev"]
